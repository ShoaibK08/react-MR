# 🛍️ Fetch Products (Fake API + Bootstrap)

---

## 📌 Install (if needed)

```bash
npm install axios bootstrap
```

---

## 📌 index.js (Bootstrap import)

```js
import "bootstrap/dist/css/bootstrap.min.css";
```

---

## 📌 App.js

```js
import React, { useEffect, useState } from "react";
import axios from "axios";

function App() {
  const [products, setProducts] = useState([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    fetchProducts();
  }, []);

  const fetchProducts = async () => {
    try {
      setLoading(true);
      const res = await axios.get("https://fakestoreapi.com/products");
      setProducts(res.data);
    } catch (err) {
      console.log(err);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="container mt-4">
      <h2 className="text-center mb-4">Products</h2>

      {loading ? (
        <p className="text-center">Loading...</p>
      ) : (
        <div className="row">
          {products.map((product) => (
            <div className="col-md-3 mb-4" key={product.id}>
              <div className="card h-100 shadow-sm">
                
                <img
                  src={product.image}
                  className="card-img-top"
                  alt={product.title}
                  style={{ height: "200px", objectFit: "contain" }}
                />

                <div className="card-body d-flex flex-column">
                  <h6 className="card-title">
                    {product.title.slice(0, 40)}...
                  </h6>

                  <p className="card-text text-success fw-bold">
                    ₹ {product.price}
                  </p>

                  <button className="btn btn-primary mt-auto">
                    Add to Cart
                  </button>
                </div>

              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}

export default App;
```

---

## ⚡ Key Points (Interview me bolna)

👉

* “Used axios for API call”
* “Handled loading state”
* “Used Bootstrap grid system”
* “Mapped products into cards”

---

## ⚠️ Common Mistakes

* key miss karna
* loading handle na karna
* UI overflow

---

## 🚀 Bonus (Agar time mila)

👉 Add:

* search
* filter
* pagination

---

# 🏁 Final Line

> "I used a fake API and displayed products using Bootstrap cards with proper state handling."
